<h1> <%= @project.title %> </h1>
<p> <small><%= @project.language %>, <%= @project.rating %>, <%= @project.genre %> 
<strong><%= link_to 'Edit', edit_project_path(@project) %> | 
<%= link_to 'Destroy', project_path(@project), method: :delete, data: { confirm: 'Are you sure you want to delete this project?' } %> </strong></small>
<p><%= @project.text %></p>
<div id="coverphoto">
	<%= image_tag @project.image_url %>
</div>
